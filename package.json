{
  "name": "basemodule",
  "version": "0.0.1",
  "main": "dist/index.js",
  "description": "Base skeleton for building ES6 ready npm modules",
  "scripts": {
    "dev": "babel --modules common src --watch --out-file dist/index.js",
    "compile": "babel --modules common src --out-file dist/index.js",
    "debug": "babel --source-maps-inline --modules common src --out-file dist/index.js",
    "test": "npm run compile && babel-node test/test-module.js | tap-spec",
    "patch": "npm version patch",
    "pack": "npm run patch && npm run compile && npm pack"
  },
  "keywords": [
    "es6",
    "babel",
    "module"
  ],
  "author": "Joani COUMA",
  "license": "ISC",
  "devDependencies": {
    "babel": "^5.3.3",
    "tap-spec": "^3.0.0",
    "tape": "^4.0.0"
  }
}
